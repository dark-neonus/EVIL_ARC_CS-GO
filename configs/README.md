# configs/

Configuration files for running simulations. Each `.cfg` file defines grid size, potential parameters, iteration limits, and output options. Configs are human-readable and allow running different experiments without recompilation.
